{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"iter8","text":"<p>iter8 is a lazy iterator library for Lua 5.1+ and LuaJIT, heavily inspired by Rust's iterators.</p> <p>Iterators are first-class objects: you can chain, transform, and consume them without evaluating until you actually need the values.</p>"},{"location":"#installation","title":"Installation","text":"<p>Copy <code>iter8.lua</code> into your project.</p>"},{"location":"#semver","title":"Semver","text":"<p>iter8 follows Semantic Versioning. The current version is 0.1.0 \u2014 the API may change before 1.0.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>local Iter8 = require \"iter8\"\n\n-- Sum the squares of the first 10 integers\nlocal result = Iter8.range(10)\n    :map(function(n) return n * n end)\n    :sum()\n-- result == 385\n\n-- Collect every other word from a string\nlocal words = Iter8.gmatch(\"a b c d e f\", \"%S+\")\n    :step_by(2)\n    :collect()\n-- words == {\"a\", \"c\", \"e\"}\n\n-- Slide a 3-element window over a list\nIter8.ivalues({1, 2, 3, 4, 5})\n    :windows(3)\n    :foreach(function(w) print(table.concat(w, \", \")) end)\n-- 1, 2, 3\n-- 2, 3, 4\n-- 3, 4, 5\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<p>The full API Reference covers every constructor and method.</p>"},{"location":"#contributing","title":"Contributing","text":"<p>Pull requests and issues are welcome.</p>"},{"location":"#running-the-test-suite","title":"Running the test suite","text":"<p>Requires busted:</p> <pre><code>luarocks install busted\nbusted spec/\n</code></pre>"},{"location":"#building-the-documentation","title":"Building the documentation","text":"<p>Requires uv:</p> <pre><code>uv run python docs/extract.py   # regenerate docs/api.md from source\nuv run mkdocs serve             # preview at http://localhost:8000\n</code></pre>"},{"location":"api/","title":"iter8 API Reference","text":"<p>iter8 is a lazy iterator library for Lua 5.1+ and LuaJIT.</p> <pre><code>local Iter8 = require \"iter8\"\n</code></pre>"},{"location":"api/#constructors","title":"Constructors","text":""},{"location":"api/#iter8","title":"<code>Iter8</code>","text":"<pre><code>Iter8(iter_fn: fun(state: any, control: any), state: any, initial: any, closing: any) \u2192 iterator\n</code></pre> <p>The main <code>Iter8</code> object.</p> <pre><code>local Iter8 = require \"iter8\"\n</code></pre> <p>When called as a function, wraps the return values of a for-loop-compatible, Lua-style iterator such as <code>pairs</code>, <code>ipairs</code>, or <code>string.gmatch</code>. The resulting <code>iterator</code> behaves transparently; that is, it provides the same results when evaluated as if you had directly evaluated the arguments to <code>Iter8</code>.</p> <p>Example: <pre><code>local iter = Iter8(pairs(t))\niter = iter:take(3)\nfor k, v in iter do\n    process(k, v)\nend\n</code></pre></p>"},{"location":"api/#iter8-range","title":"<code>Iter8.range</code>","text":"<pre><code>Iter8.range(start: integer, finish: integer, step: integer) \u2192 iterator\nIter8.range(start: integer, finish: integer) \u2192 iterator\nIter8.range(finish: integer) \u2192 iterator\n</code></pre> <p>Iterate over a range of integers. <code>start</code> and <code>step</code> are optional, both defaulting to a value of 1.</p>"},{"location":"api/#iter8-unfold","title":"<code>Iter8.unfold(seed, fn)</code>","text":"<p>Generate an <code>iterator</code> by (unfolding)[https://en.wikipedia.org/wiki/Anamorphism] using the given seed value and function.</p> <p><code>fn</code> can return either: * both of <code>seed, val</code>; * or simply <code>seed</code>, if <code>seed</code> and <code>val</code> would be the same.</p> <p>In some sense, this is the opposite of <code>iterator:fold</code>.</p> <p>Parameters:</p> Name Type <code>seed</code> <code>T</code> <code>fn</code> <code>(fun(seed: T): T) | (fun(seed: T): T, any)</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.fold</code></p>"},{"location":"api/#iter8-gmatch","title":"<code>Iter8.gmatch(s, pat)</code>","text":"<p>Iterate over matches of the Lua pattern-string <code>pat</code> in the string <code>s</code>.</p> <p>Has an alias <code>Iter8.matches</code>.</p> <p>Parameters:</p> Name Type <code>s</code> <code>string</code> <code>pat</code> <code>string</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.matches</code></p>"},{"location":"api/#iter8-matches","title":"<code>Iter8.matches</code>","text":"<p>Alias for <code>Iter8.gmatch</code>.</p>"},{"location":"api/#iter8-chars","title":"<code>Iter8.chars(s)</code>","text":"<p>Iterate over characters in the string <code>s</code>.</p> <p>Parameters:</p> Name Type <code>s</code> <code>string</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iter8-keys","title":"<code>Iter8.keys(t)</code>","text":"<p>Iterate over the keys in the table <code>t</code> in an arbitrary order (similar to <code>pairs</code>).</p> <p>Parameters:</p> Name Type <code>t</code> <code>table</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.values</code>, <code>Iter8.pairs</code></p>"},{"location":"api/#iter8-values","title":"<code>Iter8.values(t)</code>","text":"<p>Iterate over the values in the table <code>t</code> in an arbitrary order (similar to <code>pairs</code>).</p> <p>Parameters:</p> Name Type <code>t</code> <code>table</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.keys</code>, <code>Iter8.pairs</code>, <code>Iter8.ivalues</code></p>"},{"location":"api/#iter8-pairs","title":"<code>Iter8.pairs(t)</code>","text":"<p>Iterate over the keys and values in the table <code>t</code> in an arbitrary order (similar to <code>pairs</code>).</p> <p>Has an alias <code>Iter8.table</code>.</p> <ul> <li>If you just want the values, use <code>Iter8.values(t)</code> instead.</li> <li>If you just want the keys, use <code>Iter8.keys(t)</code> instead.</li> </ul> <p>Parameters:</p> Name Type <code>t</code> <code>table</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.keys</code>, <code>Iter8.values</code>, <code>Iter8.ipairs</code>, <code>Iter8.table</code></p>"},{"location":"api/#iter8-table","title":"<code>Iter8.table</code>","text":"<p>Alias for <code>Iter8.pairs</code>.</p>"},{"location":"api/#iter8-ivalues","title":"<code>Iter8.ivalues(t)</code>","text":"<p>Iterate over the values in the table <code>t</code> in increasing order (similar to <code>ipairs</code>).</p> <p>Has an alias <code>Iter8.list</code>.</p> <p>Parameters:</p> Name Type <code>t</code> <code>table</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.values</code>, <code>Iter8.pairs</code>, <code>Iter8.list</code></p>"},{"location":"api/#iter8-list","title":"<code>Iter8.list</code>","text":"<p>Alias for <code>Iter8.ivalues</code>.</p>"},{"location":"api/#iter8-ipairs","title":"<code>Iter8.ipairs(t)</code>","text":"<p>Iterate over the indices and values in the table <code>t</code> in increasing order (similar to <code>ipairs</code>).</p> <ul> <li>If you just want the values, use <code>Iter8.ivalues(t)</code> instead.</li> <li>If you just want the indices, use <code>Iter8.range(#t)</code> instead.</li> </ul> <p>Parameters:</p> Name Type <code>t</code> <code>table</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.ivalues</code>, <code>Iter8.range</code>, <code>Iter8.pairs</code></p>"},{"location":"api/#iter8-empty","title":"<code>Iter8.empty()</code>","text":"<p>Returns an empty <code>iterator</code>.</p> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iter8-once","title":"<code>Iter8.once(...)</code>","text":"<p>Returns an <code>iterator</code> which will: * on the first iteration, produce all the arguments given to <code>Iter8.once</code>; * then finish.</p> <p>Parameters:</p> Name Type <code>...</code> <code>any</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iter8-rep","title":"<code>Iter8.rep(v)</code>","text":"<p>Repeat the given value forever.</p> <p>The returned <code>iterator</code> will never finish.</p> <p>Parameters:</p> Name Type <code>v</code> <code>any</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iter8-cycle","title":"<code>Iter8.cycle(iter)</code>","text":"<p>Returns an <code>iterator</code> which endlessly repeats through the elements of the <code>iterator</code> given as the argument <code>iter</code>.</p> <p>Note</p> <p>The repetition is accomplished via memoisation of <code>iter</code>'s results; as such, if your <code>iterator</code> produces non-deterministic or otherwise changing outputs, you may wish to find another way to achieve this cycling effect.</p> <p>The returned <code>iterator</code> will never finish.</p> <p>Parameters:</p> Name Type <code>iter</code> <code>iterator</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iter8-fn","title":"<code>Iter8.fn(fn)</code>","text":"<p>Make an <code>iterator</code> from a function which uses <code>return</code> to provide the iteration step values.</p> <p>Each time the <code>iterator</code> is stepped, <code>fn</code> will be called, and any values returned will be provided as iteration step values.</p> <p>Warning</p> <p>This constructor is very low-level, and is intended only for advanced use-cases, when you need to manually implement an <code>iterator</code>.</p> <p>Unless you're sure you need this, you should probably use other functions from this library.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(): any</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.co</code></p>"},{"location":"api/#iter8-co","title":"<code>Iter8.co(fn)</code>","text":"<p>Make an <code>iterator</code> from a function which calls <code>coroutine.yield</code> to provide the iteration step values. <code>fn</code> will be passed to <code>coroutine.wrap</code>.</p> <p>Each time the <code>iterator</code> is stepped, the coroutine will be resumed, and any values passed to <code>coroutine.yield</code> will be provided as iteration step values.</p> <p>Warning</p> <p>This constructor is very low-level, and is intended only for advanced use-cases, when you need to manually implement an <code>iterator</code>.</p> <p>Unless you're sure you need this, you should probably use other functions from this library.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(): any</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>Iter8.fn</code>, <code>coroutine.wrap</code></p>"},{"location":"api/#iterator-methods","title":"iterator Methods","text":"<p>This section describes the methods available on <code>iterator</code> objects.</p>"},{"location":"api/#iterator-next","title":"<code>iterator:next()</code>","text":"<p>Return the next value in the <code>iterator</code>, or <code>nil</code> if the <code>iterator</code> is finished.</p> <p>Returns: <code>any</code></p>"},{"location":"api/#transformers","title":"Transformers","text":""},{"location":"api/#iterator-map","title":"<code>iterator:map(fn)</code>","text":"<p>Maps each step of the iterator by passing the values to <code>fn</code>, and replacing the values with the return values of <code>fn</code>.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(...): any</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.flatmap</code>, <code>iterator.filtermap</code>, <code>iterator.trace</code></p>"},{"location":"api/#iterator-trace","title":"<code>iterator:trace(fn)</code>","text":"<p>Calls <code>fn</code> on each step of the iterator, and returns the iterator unchanged. That is, <code>fn</code> is called on each step of the iterator just for its side-effects.</p> <p>Note</p> <p><code>iterator:trace(fn)</code> doesn't evaluate the <code>iterator</code>, and as such, <code>fn</code> will not be called until the <code>iterator</code> is evaluated. If you want to evaluate the iterator straight away, use <code>iterator:foreach(fn)</code>.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(...): any</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.map</code>, <code>iterator.foreach</code></p>"},{"location":"api/#iterator-filter","title":"<code>iterator:filter(pred)</code>","text":"<p>Calls <code>pred</code> on each step of the <code>iterator</code>, and keeps only the steps for which <code>pred</code> returns true.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.filtermap</code></p>"},{"location":"api/#iterator-filtermap","title":"<code>iterator:filtermap(fn)</code>","text":"<p>Maps each step of <code>iterator</code> by passing the values to <code>fn</code>, and replacing the values with the return values of <code>fn</code>\u2014as long as <code>fn</code> has a value other than <code>nil</code> as its first return value.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(...): any</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.filter</code>, <code>iterator.map</code></p>"},{"location":"api/#iterator-flatten","title":"<code>iterator:flatten()</code>","text":"<p>Turns an <code>iterator</code> of <code>iterator</code>s of values into an <code>iterator</code> of values.</p> <p><code>flatten</code> removes exactly one level of nesting.</p> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.flatmap</code></p>"},{"location":"api/#iterator-flatmap","title":"<code>iterator:flatmap(fn)</code>","text":"<p>Maps each step of <code>iterator</code> by passing the values to <code>fn</code>, and replacing the values with the concatenated results of the <code>iterator</code>s returned from <code>fn</code>.</p> <p><code>flatmap</code> removes exactly one level of nesting.</p> <p>Useful for when you want to use <code>map</code>, but your mapping function may give more than one result.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(...): iterator</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.flatten</code>, <code>iterator.map</code></p>"},{"location":"api/#iterator-take","title":"<code>iterator:take(n)</code>","text":"<p>Yields the first <code>n</code> steps of <code>iterator</code>, then ends the iterator.</p> <p>If <code>iterator</code> yields fewer than <code>n</code> steps, then the result of <code>take</code> will be equivalent to <code>iterator</code>.</p> <p>Parameters:</p> Name Type <code>n</code> <code>integer</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.drop</code></p>"},{"location":"api/#iterator-drop","title":"<code>iterator:drop(n)</code>","text":"<p>Skips the first <code>n</code> steps of <code>iterator</code>, then yields the rest of the steps of <code>iterator</code>.</p> <p>If <code>iterator</code> yields fewer than <code>n</code> steps, then the result of <code>drop</code> will be equivalent to <code>iterator.empty()</code>.</p> <p>Parameters:</p> Name Type <code>n</code> <code>integer</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.take</code></p>"},{"location":"api/#iterator-zip","title":"<code>iterator:zip(...)</code>","text":"<p>Yields the return values of all component <code>iterator</code>s as multiple values on each step of the resulting <code>iterator</code>. Ends when any one of the component <code>iterator</code>s ends; that is, the result of <code>zip</code> is the same length as the shortest component <code>iterator</code>.</p> <p>All return values of component <code>iterator</code>s are included, including multiple return values of component <code>iterator</code>s.</p> <p>Joins multiple <code>iterator</code>s \"in parallel\". To join <code>iterator</code>s \"in series\", use <code>iterator:chain(...)</code>.</p> <p>Example: <pre><code>-- prints: \"h\" 1; \"e\" 2; \"l\" 3; \"l\" 4; \"o\" 5\nIter8.chars(\"hello\"):zip(Iter8.range(10)):foreach(print)\n\n-- prints: \"a\" 1 true; \"b\" 2 true; \"c\" 3 true\nIter8.chars(\"abc\"):zip(Iter8.range(10), Iter8.rep(true)):foreach(print)\n</code></pre></p> <p>Parameters:</p> Name Type <code>...</code> <code>iterator</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.enumerate</code>, <code>iterator.select</code>, <code>iterator.chain</code></p>"},{"location":"api/#iterator-enumerate","title":"<code>iterator:enumerate()</code>","text":"<p>Adds the step index as an extra first value.</p> <p>Equivalent to <code>Iter8.range(math.maxinteger):zip(iterator)</code>.</p> <p>Example: <pre><code>-- prints: 1 \"a\"; 2 \"b\"; 3 \"c\"\nfor i, v in Iter8.chars(\"abc\"):enumerate() do\n    print(i, v)\nend\n</code></pre></p> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.zip</code></p>"},{"location":"api/#iterator-select","title":"<code>iterator:select(index)</code>","text":"<p>Yields a single return value from each of <code>iterator</code>'s steps, ignoring the others. The argument is chosen by <code>index</code>: * If <code>index == 1</code>, the first argument is returned; * If <code>index == 2</code>, the second argument is returned; * etc. In particular, if <code>index</code> is greater than the number of arguments, the returned iterator will be empty.</p> <p><code>iterator:select(index)</code> is analogous to Lua's <code>select(index, ...)</code>.</p> <p>This might be thought of as a way to \"undo\" <code>zip</code>.</p> <p>Parameters:</p> Name Type <code>index</code> <code>integer</code> <p>See also: <code>iterator.zip</code></p>"},{"location":"api/#iterator-chain","title":"<code>iterator:chain(...)</code>","text":"<p>Yields all the values of <code>iterator</code>, then all the values of the first argument, then all the values of the second argument, and so forth; until all the <code>iterator</code>s are finished.</p> <p>Joins multiple <code>iterator</code>s \"in series\". To join <code>iterator</code>s \"in parallel\", use <code>iterator:zip(...)</code>.</p> <p>Parameters:</p> Name Type <code>...</code> <code>iterator</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.zip</code></p>"},{"location":"api/#iterator-takewhile","title":"<code>iterator:takewhile(pred)</code>","text":"<p>Yields elements while <code>pred</code> returns true, stopping at the first element for which <code>pred</code> returns false.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.dropwhile</code>, <code>iterator.filter</code></p>"},{"location":"api/#iterator-dropwhile","title":"<code>iterator:dropwhile(pred)</code>","text":"<p>Skips elements while <code>pred</code> returns true, then yields all remaining elements.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.takewhile</code>, <code>iterator.drop</code></p>"},{"location":"api/#iterator-scan","title":"<code>iterator:scan(acc, fn)</code>","text":"<p>Like <code>fold</code>, but yields each intermediate accumulator value rather than returning only the final result.</p> <p>Parameters:</p> Name Type <code>acc</code> <code>A</code> <code>fn</code> <code>fun(acc: A, x: T): A</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.fold</code></p>"},{"location":"api/#iterator-chunks","title":"<code>iterator:chunks(n)</code>","text":"<p>Yields non-overlapping chunks of <code>n</code> elements as tables. The final chunk may be smaller than <code>n</code> if the iterator does not divide evenly.</p> <p>Parameters:</p> Name Type <code>n</code> <code>integer</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.windows</code></p>"},{"location":"api/#iterator-windows","title":"<code>iterator:windows(n)</code>","text":"<p>Yields every overlapping window of <code>n</code> consecutive elements as a table. Produces <code>max(0, length - n + 1)</code> windows.</p> <p>Parameters:</p> Name Type <code>n</code> <code>integer</code> <p>Returns: <code>iterator</code></p> <p>See also: <code>iterator.chunks</code></p>"},{"location":"api/#iterator-step-by","title":"<code>iterator:step_by(n)</code>","text":"<p>Yields every <code>n</code>th element, starting with the first.</p> <p><code>step_by(1)</code> is equivalent to the original iterator.</p> <p>Parameters:</p> Name Type <code>n</code> <code>integer</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#iterator-intersperse","title":"<code>iterator:intersperse(sep)</code>","text":"<p>Inserts <code>sep</code> between each element of <code>iterator</code>.</p> <p>Parameters:</p> Name Type <code>sep</code> <code>any</code> <p>Returns: <code>iterator</code></p>"},{"location":"api/#terminators","title":"Terminators","text":""},{"location":"api/#iterator-force","title":"<code>iterator:force()</code>","text":"<p>Evaluates <code>iterator</code> for its side effects, throwing away its return values.</p> <p>See also: <code>iterator.collect</code>, <code>iterator.foreach</code></p>"},{"location":"api/#iterator-foreach","title":"<code>iterator:foreach(fn)</code>","text":"<p>Evaluates <code>iterator</code>, calling <code>fn</code> on each step's values, and throwing away <code>fn</code>'s return values.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(...)</code> <p>See also: <code>iterator.trace</code>, <code>iterator.map</code>, <code>iterator.force</code></p>"},{"location":"api/#iterator-collect","title":"<code>iterator:collect()</code>","text":"<p>Evaluates <code>iterator</code>, collecting all result values into a table.</p> <ul> <li>If <code>iterator</code> has one return value per step,   the resulting table will be list-like   (i.e. keys from 1, 2, 3...).</li> <li>If <code>iterator</code> has two or more return values per step,   the resulting table will be map-like   (i.e. keys from the first return value,    values from the second return value).   Note that all other arguments will be ignored.</li> </ul> <p>If you just want to evaluate <code>iterator</code>, and don't care about the results, use <code>iterator:force()</code> instead.</p> <p>Returns: <code>table</code></p> <p>See also: <code>iterator.force</code></p>"},{"location":"api/#iterator-fold","title":"<code>iterator:fold(acc, fn)</code>","text":"<p>Evaluates <code>iterator</code>, folding the return values into the accumulator <code>acc</code> using the folding function <code>fn</code>.</p> <p>On each step, <code>fn</code> is called like so: <code>acc = fn(x, acc)</code> (where <code>x</code> is the current value of <code>iterator</code>,  and <code>acc</code> is the previously-returned value of <code>acc</code>).</p> <p>The result returned from <code>fold</code> is the final value of <code>acc</code>.</p> <p>In some sense, this is the opposite of <code>Iter8.unfold</code>.</p> <p>Parameters:</p> Name Type <code>acc</code> <code>A</code> <code>fn</code> <code>fun(x:</code>T<code>, acc: A): A</code> <p>Returns: <code>A</code></p> <p>See also: <code>iterator.fold1</code>, <code>Iter8.unfold</code></p>"},{"location":"api/#iterator-fold1","title":"<code>iterator:fold1(fn)</code>","text":"<p>Evaluates <code>iterator</code>, folding the return values into the accumulator using the folding function <code>fn</code>.</p> <p>The accumulator is initialised with the first return value of the first step of <code>iterator</code>, and then the rest of the steps are folded in. This is useful for when you know that <code>iterator</code> contains at least one value, and you don't want to\u2014or are unable to\u2014provide a starting accumulator value.</p> <p>If <code>iterator</code> is empty, <code>fold1</code> returns <code>nil</code>.</p> <p>See the documentation of <code>fold</code> for more information on how folding works.</p> <p>Parameters:</p> Name Type <code>fn</code> <code>fun(x:</code>T<code>, acc:</code>A<code>): A</code> <p>Returns: <code>A</code></p> <p>See also: <code>iterator.fold</code></p>"},{"location":"api/#iterator-concat","title":"<code>iterator:concat(sep)</code>","text":"<p>Evaluates <code>iterator</code>, returning the result of string-concatenating each value of <code>iterator</code>.</p> <p>An optional <code>sep</code> string is inserted between each element.</p> <p>Parameters:</p> Name Type <code>sep?</code> <code>string</code> <p>Returns: <code>string</code></p>"},{"location":"api/#iterator-any","title":"<code>iterator:any(pred)</code>","text":"<p>Evaluates <code>iterator</code>, returning <code>true</code> if <code>pred</code> returns true for at least one element. Short-circuits on the first matching element.</p> <p>Returns <code>false</code> for an empty iterator.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>boolean</code></p> <p>See also: <code>iterator.all</code></p>"},{"location":"api/#iterator-all","title":"<code>iterator:all(pred)</code>","text":"<p>Evaluates <code>iterator</code>, returning <code>true</code> if <code>pred</code> returns true for every element. Short-circuits on the first non-matching element.</p> <p>Returns <code>true</code> for an empty iterator.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>boolean</code></p> <p>See also: <code>iterator.any</code></p>"},{"location":"api/#iterator-find","title":"<code>iterator:find(pred)</code>","text":"<p>Evaluates <code>iterator</code>, returning the first element for which <code>pred</code> returns true, or <code>nil</code> if no element matches.</p> <p>Parameters:</p> Name Type <code>pred</code> <code>fun(...): boolean</code> <p>Returns: <code>any</code></p>"},{"location":"api/#iterator-min","title":"<code>iterator:min()</code>","text":"<p>Evaluates <code>iterator</code>, returning the minimum value. Returns <code>nil</code> for an empty iterator.</p> <p>Returns: <code>any</code></p> <p>See also: <code>iterator.max</code></p>"},{"location":"api/#iterator-max","title":"<code>iterator:max()</code>","text":"<p>Evaluates <code>iterator</code>, returning the maximum value. Returns <code>nil</code> for an empty iterator.</p> <p>Returns: <code>any</code></p> <p>See also: <code>iterator.min</code></p>"},{"location":"api/#iterator-count","title":"<code>iterator:count()</code>","text":"<p>Evaluates <code>iterator</code>, returning the number of steps of <code>iterator</code>.</p> <p>Returns: <code>integer</code></p>"},{"location":"api/#iterator-sum","title":"<code>iterator:sum()</code>","text":"<p>Evaluates <code>iterator</code>, returning the numeric sum of each value of <code>iterator</code>.</p> <p>Returns: <code>number</code></p>"},{"location":"api/#iterator-nth","title":"<code>iterator:nth(n)</code>","text":"<p>Evaluates <code>iterator</code>, returning the yielded values of the \"nth\" step of <code>iterator</code>.</p> <p>Like many things in Lua, <code>nth</code> is 1-based; i.e. <code>iterator:nth(1)</code> will return the first step of <code>iterator</code>.</p> <p>If <code>n</code> is greater than the number of steps of <code>iterator</code>, <code>nth</code> returns <code>nil</code>.</p> <p>Returns: <code>any</code></p>"},{"location":"api/#iterator-last","title":"<code>iterator:last()</code>","text":"<p>Evaluates <code>iterator</code>, returning the yielded values of the final step of <code>iterator</code>.</p> <p>If <code>iterator</code> is empty, <code>last</code> returns <code>nil</code>.</p> <p>Returns: <code>any</code></p>"}]}